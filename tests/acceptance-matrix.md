# Skill 验收矩阵

版本日期：2026-09-06

本表只登记实际运行过的检查，不把规则存在、文件存在或另一个数据库通过当作真实验收。

| 范围 | 实际证据 | 结果 | 当前边界 |
| --- | --- | --- | --- |
| Skill 结构 | 使用 `skill-creator` 的 `quick_validate.py` 检查名称、frontmatter 和目录结构 | 通过 | 中文 Windows 需以 UTF-8 模式运行验证器 |
| 自动测试 | 运行 `tests/run_all.ps1`，覆盖配置、可移植性、下载包、来源清单、问卷证据、问卷路径、身份值保留、BOM、多表恢复、可读性发布门、R runner 和摘要组件 | `ALL_SKILL_TESTS_PASS` | 自动测试验证结构和失败边界，不代替真实主题阅读 |
| 问卷展示模板与兼容 | 公共 helper 同时覆盖结构化进入条件和既有题前说明：回答选项独立使用 `0.8em/#888888`，进入条件与跳题说明独立使用 `0.72em/正文深色`；外层只负责换行和缩进，不再用嵌套 `em` 计算字号。CHARLS 035 的13处既有题前说明和036的42条结构化进入条件均纳入正式重生成 | 自动测试检查选项与跳题的字号互不继承；035的6个变量、036的8个变量继续与各自审核基准逐项核对 | 进入条件只说明哪些对象回答本题；真正离开本主题才写“结束该主题”。既有旧调用保持兼容，不据此自动重写未重生成主题 |
| 角色与模型路由 | CHARLS 工作属性重做已实际采用公开 R 和普通读者只读 subagent，主执行者负责修改、运行和网站；过程报告记录各角色及耗时 | 分工已实际执行，效率未通过 | 来源阶段未完整计时，存在环境排错和重复阅读；本次收口修正已通过专项测试，尚未证明下一次真实任务提速 |
| 运行与报告收口 | 全套测试及官方 quick validator 通过；新增配置驱动 runner、无效配置拒绝回退、报告历史保留、重复阶段/未决问题阻断和工作/等待/返工分类 | 自动测试通过 | 只运行隔离 fixture，未重跑现有主题，未以此声称线上流程耗时达标 |
| 网站入口与确定性执行 | `verify-ready` 先在计时外完成登录、标签绑定和固定 CUA 程序预载；`verify-ready --start-sync` 再启动报告并只返回短执行指令。两步必须核对同一个程序哈希，开始计时后不得插入检查或排错；超过 60 秒仍未进入固定程序时，在任何网站写入前拒绝执行。程序复用现有内置浏览器标签页，核对编辑页和标题，正文只导入一次，侧栏附件按 `analysis_db`、`analysis_codebook` 逐个上传并核对顺序，最后确认返回文章页 | 自动测试、失败边界及 036 真实原样回归均通过；回归调度 `13.798` 秒、浏览器 `3.313` 秒、同步启动至返回 `17.111` 秒、网站阶段墙钟 `46` 秒 | 分别记录调度、浏览器五段动作、同步启动至返回和网站阶段墙钟；登录与预载在计时外完成，不能拿浏览器动作时间冒充整个阶段耗时 |
| 未变笔记审核保留 | fixture 验证分析附件变化而笔记完全未变时保留合格读者审核；笔记变化时拒绝保留，发布许可仍重新生成 | 通过 | 不自动判断语义等价，不自动填写作者或读者结论 |
| 下载前后路径检查分工 | 下载入口允许尚待实测的 null 计数；正式验收仍要求实测计数和闭合结论。独立复核发现并修正了 null 计数与 true 闭合标志可并存的问题；四组合及首尾分支检查通过，修补后再次运行全套测试通过 | 通过 | 下载计划通过不代表实际数据路径闭合；不以该测试声称036扩展结果已完成 |
| CHARLS 036 范围与链路 | 从完整问卷路径重新审查工作单位与岗位属性，最终定义雇主类型、企业所有制、公务员、政府编制、事业编制、职务、职称和管理职责 8 个变量；31 个唯一来源、36 个原题时期和4个未问时期均已登记，035上游只用于适用性核对，不作为删除036有效回答的硬条件 | 来源、问卷路径、公开R、正式输出、读者材料和网站前向验证均通过 | 8项的适用对象不同；未进入相应分支形成的空白不能直接解释为“否”或“其他” |
| CHARLS 真实主题 A | 覆盖重新下载、复杂跳题路径、跨期来源、初学者可读 R、完整文案审核和网站同步 | 通过 | 早期探索形成于 Skill 建立前，正式成果已使用本 Skill 重生与检查 |
| CHARLS 真实主题 B | 覆盖来源用途筛选、家庭一致性验证、官方构建指标直用、defined-only 渲染、完整文案审核和网站同步 | 通过 | 实际执行暴露并推动了来源用途与分类检查的公共修正 |
| ELSA | 已有 ELSA 路由、profile、配置和 Wave 文本组件测试 | 本轮未做真实主题前向验收 | 按用户决定暂不阻塞当前 Skill 收口；首次实际使用时在正式仓库直接修正发现的问题 |
| 旧本地框架收口 | 数据库主题色移入 `references/database-themes.json` 并加入路由校验；37 个仍依赖旧 `_工具` 的现行正式 R 改由 Skill runner 提供公共脚本，42 个现行正式 R 全部通过语法解析；公众号 SVG、封面与富文本从已安装 Skill 读取主题参数，CHARLS/ELSA 实际载入通过；现行规则、正式源和宣传代码中的旧框架引用清零 | 通过 | `_执行线程` 和 `archived_source` 中的旧路径是历史记录，不作为当前运行入口，也不回写历史证据 |
| 公开与跨设备 | 本机配置由 `config.local.json` 承载并被忽略；共享规则不含本机路径、账号或本地域名；官方材料由 manifest 登记；Git 文本行尾固定为 LF。官方 quick validator 与全套测试均通过；正式仓库同步到 [ZhidoHuang/dbcodebook-definition](https://github.com/ZhidoHuang/dbcodebook-definition) | 通过 | 新设备需安装 Skill，并自行创建 `config.local.json` 配置 Python、R 与网站地址 |

当前结论：基础能力、跨设备隔离副本和网站同步的确定性执行程序均已通过自动测试。发布收口审计发现身份检查曾机械要求 raw 与分析表逐行等长，会误拒绝合法的筛选、排序和长转宽；现已改为核对分析结果中的身份组合确实来自 raw，并用宽表、乱序、前导0丢失和凭空身份四类情形完成回归。此前 036 网站回归中，浏览器动作只有 `3.135` 秒，但同步启动至浏览器返回为 `400.351` 秒、网站阶段墙钟为 `462` 秒，证明耗时来自计时后的调度漂移。修正为“计时外预载固定程序、计时后只发送短指令”后，036 原样回归的调度为 `13.798` 秒、浏览器为 `3.313` 秒、同步启动至文章页返回为 `17.111` 秒、网站阶段墙钟为 `46` 秒，五项网站质量检查全部通过。该回归只重复同步同一份正式正文和两份附件，没有改变主题内容或数据结果。ELSA 的真实成功路径留到首次正式使用时验证。
