# 定义成果机器闭环与按需验收规则

正式源说明：本文件是 CHARLS、ELSA 及后续数据库共用的唯一验收规则。独立验收不是定义流水线的固定工位，不得因任务类型、改动类型或历史习惯自动启动。

## 1. 默认路径：执行任务机器闭环

主题定义任务同时负责生成与验证。完成以下机器闸门后先标记 `MACHINE_CHECK_PASS`：

在正式下载和编写正式 R 之前，必须由区别于主执行者的短时只读角色完成一次定义逻辑复核。复核只读取当前 `探索记录.md`、`definition_search_record.json`、已定位的权威材料和必要的原始题目，不重新探索整个主题；逐项检查来源是否完整、定义是否片面、问卷路径是否闭合、周期与对象是否准确、分析单位和身份键是否合适，以及每个来源变量是否实际用于定义、辅助判定或验证。复核者只返回具体发现，不修改文件。主执行者负责处理发现，并把结果写入已有的 `logic_review` 和 `logic_issues`，存在未解决问题时不得进入下载和正式 R。

这项固定复核不是第 2 节所说的独立验收：它不重复生产、不另建过程卡，也不负责替用户裁决研究口径。运行环境暂时不能建立独立只读角色时，主执行者必须采用隔离上下文重新检查上述项目，并在汇报中明确这一限制，不能把原执行过程中的顺手自查称为独立复核。

下载前核对计划中的进入条件、题目路径、所需来源和定义结果，不要求尚未下载的数据已有实际统计。路径的 `observed_count`、`unexplained_count` 及实际闭合标记在未核对时保留 `null`，不能预填0或通过。下载后用本次实际数据补齐人数并核对每条路径；正式运行与最终来源验收仍要求人数有效、未解释人数为0及实际路径闭合。下载前通过只允许提取，不表示结果已验收。

“定义是否片面”分两层：先依据用户主题和已定位的问卷/官方材料判断应覆盖哪些研究概念，说明主要候选为何纳入、另属哪个主题或暂不纳入；再检查每个已选变量的构造是否完整。取舍写入现有 `candidate_decisions` 和探索记录，不新增记录体系。围绕既定结果筛选原始字段，只证明这些结果的来源用途，不证明主题范围充分；重做也不能把旧变量集合默认为已批准的完整范围。

1. 正式 runner 成功，唯一最终日志含明确退出码 0。
2. 固定检查器使用正确数据库参数运行并为 `ok: true`。
3. 正式目录、raw/header/codebook、analysis、mapping、禁止变量、唯一日志和用户材料结构检查通过。问卷展示在已有全文审核中按 [common-materials.md](common-materials.md) 第 6.2 节模板核对，确保题前条件、完整选项及跳转说明清楚且与原问卷一致。
4. 按实际改动完成足够的回归或独立复算，能够证明变量值、缺失、覆盖、来源身份和输出结构符合当前裁决；已发现的原始题目间矛盾必须证明各变量按直接来源分别保留，未经用户裁决没有发生静默协调。对每个定义变量和调查时期，还必须把进入条件、直接回答、预载信息、确认与更新、跳题方向及最终出口逐条连接到定义结果和所需原始变量，并用实际数据统计每条路径。存在未处理的观察路径、无法解释的结构性缺失或尚未提取的必要字段时，机器闸门不得通过。
5. 新增或修改公共机制时，相关 fixture、专项扫描和既有主题回归通过。
6. 过程证据存在、非空、可读；正式目录不混入过程材料。

机器闸门任一失败时，执行任务必须自行修正或向大脑报告阻断。不得把未闭环成果转交验收任务代为排错。



机器闸门通过后，按通用规范先完成作者全文检查，再完成普通读者检查。主题作者生成 `readability_audit.json`；另一审核角色只阅读最终笔记并生成 `reader_comprehension_review.json`，逐块保存原文和大白话复述。`check_definition_readability.py check` 验证纯文案、两份审核及当前正式笔记、正式 R、analysis_db 和 analysis_codebook 哈希一致后，生成 `publish_readiness.json`。没有当前版本的 `PUBLISH_READY` 不得同步网站。请求包含网站更新时，按写入边界规则直接进入网站检查同步阶段，提交并收到文章详情页后进入 `USER_REVIEW_PENDING`；发布后不再重复检查页面，只有用户确认才算完成。

### 修正后的复核范围

首次完整生成先完成机器验证，再做两轮阅读。之后按实际影响回到相应步骤：来源或别名变化重新下载全量来源并从头运行；计算逻辑变化从头运行并验证受影响结果；仅表达变化重新生成并核对文字及结果未被改变，不重新探索或下载。相应只读角色复核它负责的改动及上下游关系，不重复未受影响的来源调查。

任何绑定成果变化都会使发布许可失效，作者必须重新绑定当前文件并记录变更影响。最终笔记有变化时重新全文阅读；最终笔记文件完全相同、仅 R 后台区或分析附件变化时，作者核对受影响成果和它与正文的一致性，可引用未变部分原有的全文审核证据。此时 `init --overwrite --preserve-reader` 会验证并保留同一笔记已有的合格读者审核，不重新派普通读者。笔记哈希、审核状态或逐块证据不符时该选项失败，不会自动补成通过。最终仍须重新运行 `check` 取得当前发布许可。

## 2. 独立验收仅有四个入口

只有出现以下情况之一，才启动独立只读验收：

1. 机器证据缺失、失败、互相矛盾，或出现执行任务无法解释的差异。
2. 存在当前规则和用户裁决无法解决的研究判断，例如来源语义、对象边界、计分口径或多来源 mismatch 仍有歧义。
3. 公共核心机制发生高影响修改，且现有 fixture、回归和机器复算不足以证明影响边界。
4. 用户或大脑明确要求独立验收。

新数据库、新主题、raw/transaction、变量集合、命名、顺序、schema、编码、mapping 或公共工具发生变化，本身都不是自动验收理由。只要口径已经裁决，机器证据完整且结果可解释，仍按默认机器闭环完成。

不同原始题目之间存在矛盾，但各定义变量仍可按自己的直接来源忠实生成时，也不是自动验收或 `RESULT_REVIEW_REQUIRED` 理由。执行任务应保留矛盾、保存冲突清单并在读者材料中披露后继续。只有单个变量本身的生成规则无法确定，或存在来源不明、重复计入、多来源覆盖规则不明等实质定义问题时，才按第 2 项进入研究判断裁决。

取消常规质量抽查，不按任务数量随机启动验收。

## 3. 独立验收的职责边界

独立验收只回答触发它的那个未决问题，不重复读取和核验执行任务已经机器证明的全部成果。

- 机器证据冲突：只复核冲突字段和对应生成链。
- 研究判断未决：只核证据边界并回大脑裁决，不替用户决定口径。
- 公共核心机制风险：只核受影响机制及代表性回归主题。
- 用户指定：严格按用户指定范围执行。

独立验收不得重新跑完整生产流程，不得把文件清单、日志、目录、普通文本同步和常规 xlsx 读取重新做一遍。

## 4. 状态与入账

- 默认路径：`GENERATED -> MACHINE_CHECK_PASS -> AUTHOR_FULL_TEXT_REVIEW -> ORDINARY_READER_REVIEW -> PUBLISH_READY -> USER_REVIEW_PENDING -> COMPLETE`。
- 异常路径：`GENERATED -> MACHINE_CHECK_PASS/BLOCKED -> SENT_FOR_VALIDATION -> VALIDATION_PASS -> AUTHOR_FULL_TEXT_REVIEW -> ORDINARY_READER_REVIEW -> PUBLISH_READY -> USER_REVIEW_PENDING -> COMPLETE`。
- machine closure 只需记录机器报告路径和是否存在上述四类异常；无异常时写 `independent_validation_required=false`，不要求另建过程卡。
- `MACHINE_CHECK_PASS` 说明机器证据闭环；`PUBLISH_READY` 说明作者检查和普通读者检查均已完成，且检查器确认两份审核记录和四份成果一致，可以同步网站；`USER_REVIEW_PENDING` 说明成果已同步并等待用户阅读；只有用户确认后才标记 `COMPLETE`。这些状态不能写成同一时点的并列状态。

独立验收任务保留为异常调查工具，不参与常规定义闭环。
